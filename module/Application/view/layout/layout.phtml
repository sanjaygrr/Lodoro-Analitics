<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 */
?>
<?= $this->doctype() ?>
<html lang="es">
    <head>
        <meta charset="utf-8">
        <?= $this->headTitle('Lodoro Analytics')->setSeparator(' - ')->setAutoEscape(false) ?>

        <?= $this->headMeta()
            ->appendName('viewport', 'width=device-width, initial-scale=1.0')
            ->appendHttpEquiv('X-UA-Compatible', 'IE=edge')
        ?>

        <!-- Favicon y estilos base -->
        <?= $this->headLink([
                'rel'  => 'shortcut icon',
                'type' => 'image/vnd.microsoft.icon',
                'href' => $this->basePath() . '/img/favicon.ico'
            ])
            ->prependStylesheet($this->basePath('css/style.css'))
            ->prependStylesheet($this->basePath('css/bootstrap.min.css'))
        ?>

        <!-- Font Awesome para iconos -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
        
        <!-- Google Fonts - Inter -->
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap">

        <!-- Estilos personalizados corregidos -->
        <style>
            :root {
                --primary: #4361ee;
                --secondary: #6c757d;
                --success: #4caf50;
                --danger: #f44336;
                --warning: #ff9800;
                --info: #2196f3;
                --dark: #1e2843;
                --darker: #151c30;
                --light: #f8f9fa;
                --white: #ffffff;
                --body-bg: #f5f7fb;
                --text-main: #3b3f5c;
                --text-muted: #6c757d;
                --text-light: #cbd5e1;
                --border-color: #e0e6ed;
                --shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
                --transition-speed: 0.3s;
                --font-family: 'Inter', sans-serif;
            }

            body {
                font-family: var(--font-family);
                background-color: var(--body-bg);
                margin: 0;
                padding: 0;
                overflow-x: hidden;
            }

            /* Contenedor principal */
            .app-container {
                display: flex;
                min-height: 100vh;
            }

            /* Sidebar - CORREGIDO */
            #sidebar {
                background: linear-gradient(180deg, var(--dark) 0%, var(--darker) 100%);
                color: var(--white);
                min-height: 100vh;
                transition: all var(--transition-speed) ease; /* Cambiado de width a all para mejor transición */
                position: fixed;
                top: 0;
                left: 0;
                z-index: 1000;
                box-shadow: var(--shadow);
                overflow-y: auto;
                width: 70px; /* Estado inicial colapsado */
            }

            /* Control de anchos - CORREGIDO */
            .sidebar-collapsed {
                width: 70px !important;
            }

            .sidebar-expanded {
                width: 240px !important;
            }

            /* Cabecera del sidebar */
            .sidebar-header {
                padding: 1rem;
                display: flex;
                align-items: center;
                height: 70px;
                border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            }

            .logo-icon {
                font-size: 1.5rem;
                color: var(--primary);
                width: 30px;
                text-align: center;
            }

            .logo-text {
                margin-left: 0.75rem;
                font-size: 1.25rem;
                font-weight: 600;
                white-space: nowrap;
                opacity: 0;
                transition: opacity var(--transition-speed) ease;
            }

            .sidebar-expanded .logo-text {
                opacity: 1;
            }

            .toggle-btn {
                margin-left: auto;
                background: transparent;
                border: none;
                color: var(--text-light);
                cursor: pointer;
                width: 28px;
                height: 28px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                transition: all var(--transition-speed) ease;
            }

            .toggle-btn:hover {
                background-color: rgba(255, 255, 255, 0.1);
                color: var(--white);
            }

            /* Menú de navegación */
            .nav-menu {
                list-style: none;
                padding: 1rem 0;
                margin: 0;
            }

            .menu-header {
                padding: 0.5rem 1rem;
                font-size: 0.75rem;
                text-transform: uppercase;
                color: var(--text-light);
                font-weight: 600;
                opacity: 0;
                transition: opacity var(--transition-speed) ease;
                white-space: nowrap;
            }

            .sidebar-expanded .menu-header {
                opacity: 1;
            }

            .nav-item {
                margin: 0.25rem 0;
                position: relative;
            }

            .nav-link {
                display: flex;
                align-items: center;
                padding: 0.75rem 1rem;
                color: var(--text-light);
                text-decoration: none;
                position: relative;
                transition: all var(--transition-speed) ease;
                border-radius: 0.25rem;
                margin: 0 0.5rem;
            }

            .nav-link:hover, .nav-link.active {
                color: var(--white);
                background-color: rgba(255, 255, 255, 0.1);
            }

            .nav-link.active::before {
                content: '';
                position: absolute;
                left: 0;
                top: 0;
                height: 100%;
                width: 3px;
                background-color: var(--primary);
                border-radius: 0 2px 2px 0;
            }

            .nav-icon {
                font-size: 1.25rem;
                min-width: 30px;
                text-align: center;
            }

            .nav-text {
                margin-left: 0.75rem;
                white-space: nowrap;
                opacity: 0;
                transition: opacity var(--transition-speed) ease;
                display: inline-block; /* AÑADIDO para asegurar que el texto sea visible */
                font-weight: 500; /* Mejorar legibilidad */
            }

            .sidebar-expanded .nav-text {
                opacity: 1;
                visibility: visible;
            }
            
            /* Estado expandido para mostrar todos los títulos */
            .sidebar-expanded .menu-header,
            .sidebar-expanded .nav-text,
            .sidebar-expanded .badge-new {
                opacity: 1;
                visibility: visible;
                display: inline-block;
            }

            /* Tooltip estilizado - CORREGIDO para asegurar visibilidad */
            .tooltip-nav {
                position: absolute;
                left: 70px; /* Coincide con el ancho del sidebar colapsado */
                top: 50%;
                transform: translateY(-50%);
                background-color: rgba(0, 0, 0, 0.9);
                color: white;
                padding: 0.5rem 0.75rem;
                border-radius: 0.25rem;
                font-size: 0.8rem;
                white-space: nowrap;
                pointer-events: none;
                opacity: 0;
                visibility: hidden;
                z-index: 9999; /* Z-index alto para asegurar que esté por encima de todo */
                box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
            }

            .tooltip-nav::before {
                content: '';
                position: absolute;
                right: 100%;
                top: 50%;
                transform: translateY(-50%);
                border-width: 6px;
                border-style: solid;
                border-color: transparent rgba(0, 0, 0, 0.9) transparent transparent;
            }

            /* CORREGIDO: Regla específica para mostrar tooltips en sidebar colapsado */
            .sidebar-collapsed .nav-item:hover .tooltip-nav {
                opacity: 1;
                visibility: visible;
                display: block;
            }

            .badge-new {
                background-color: var(--primary);
                color: white;
                padding: 0.15rem 0.5rem;
                border-radius: 10px;
                font-size: 0.65rem;
                margin-left: auto;
                opacity: 0;
                transition: opacity var(--transition-speed) ease;
            }

            .sidebar-expanded .badge-new {
                opacity: 1;
            }

            /* Insignia de notificación - CORREGIDA */
            .notification-badge {
                position: absolute;
                top: 0;
                right: 0;
                background-color: var(--danger);
                color: white;
                font-size: 0.65rem;
                font-weight: 600;
                width: 16px;
                height: 16px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                z-index: 1;
            }

            /* Separador */
            .menu-divider {
                height: 1px;
                background-color: rgba(255, 255, 255, 0.1);
            }

            /* Panel de notificaciones */
            .notifications-panel {
                position: fixed;
                top: 0;
                right: -320px;
                width: 320px;
                height: 100vh;
                background-color: var(--white);
                box-shadow: var(--shadow);
                z-index: 1001;
                transition: right var(--transition-speed) ease;
                overflow-y: auto;
            }

            .notifications-panel.open {
                right: 0;
            }

            .notifications-header {
                padding: 1rem;
                background-color: var(--primary);
                color: white;
                display: flex;
                align-items: center;
                justify-content: space-between;
            }

            .notifications-title {
                font-size: 1.1rem;
                font-weight: 600;
            }

            .close-notifications {
                background: transparent;
                border: none;
                color: white;
                cursor: pointer;
                width: 28px;
                height: 28px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
            }

            .close-notifications:hover {
                background-color: rgba(255, 255, 255, 0.2);
            }

            .notifications-tabs {
                display: flex;
                border-bottom: 1px solid var(--border-color);
            }

            .notifications-tab {
                flex: 1;
                padding: 0.75rem;
                text-align: center;
                cursor: pointer;
                border-bottom: 2px solid transparent;
                color: var(--text-muted);
                font-weight: 500;
                transition: all var(--transition-speed) ease;
            }

            .notifications-tab.active {
                border-bottom-color: var(--primary);
                color: var(--primary);
            }

            .notifications-content {
                padding: 0;
            }

            .notification-item {
                padding: 1rem;
                border-bottom: 1px solid var(--border-color);
                display: flex;
                align-items: center;
                transition: background-color 0.2s ease;
            }

            .notification-item:hover {
                background-color: rgba(67, 97, 238, 0.05);
            }

            .notification-icon {
                width: 36px;
                height: 36px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                margin-right: 0.75rem;
                flex-shrink: 0;
            }

            .notification-success {
                background-color: rgba(76, 175, 80, 0.1);
                color: var(--success);
            }

            .notification-info {
                background-color: rgba(33, 150, 243, 0.1);
                color: var(--info);
            }

            .notification-warning {
                background-color: rgba(255, 152, 0, 0.1);
                color: var(--warning);
            }

            .notification-danger {
                background-color: rgba(244, 67, 54, 0.1);
                color: var(--danger);
            }

            .notification-content {
                flex: 1;
            }

            .notification-title {
                font-weight: 500;
                font-size: 0.875rem;
                margin-bottom: 0.25rem;
                color: var(--text-main);
            }

            .notification-desc {
                font-size: 0.75rem;
                color: var(--text-muted);
                margin-bottom: 0.25rem;
            }

            .notification-time {
                font-size: 0.7rem;
                color: var(--text-muted);
            }

            .notification-actions {
                margin-top: 0.5rem;
            }

            .notification-btn {
                font-size: 0.75rem;
                padding: 0.15rem 0.5rem;
                border-radius: 0.25rem;
                border: none;
                cursor: pointer;
                margin-right: 0.5rem;
                background-color: var(--primary);
                color: white;
            }

            .notification-all {
                display: block;
                text-align: center;
                padding: 0.75rem;
                background-color: var(--light);
                color: var(--text-main);
                text-decoration: none;
                font-size: 0.875rem;
                font-weight: 500;
            }

            .notification-all:hover {
                background-color: rgba(67, 97, 238, 0.05);
            }

            /* Overlay para notificaciones en móvil */
            .overlay-notifications {
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background-color: rgba(0, 0, 0, 0.5);
                z-index: 1000;
                opacity: 0;
                visibility: hidden;
                transition: opacity var(--transition-speed) ease;
            }

            .overlay-notifications.open {
                opacity: 1;
                visibility: visible;
            }

            /* Botón de expansión inferior */
            .sidebar-bottom-toggle {
                padding: 0.5rem 1rem;
                display: flex;
                justify-content: center;
                flex-shrink: 0; /* No permitir que se encoja */
                margin-top: auto; /* Empujar al fondo */
            }
            
            .toggle-btn-bottom {
                background: transparent;
                border: none;
                color: var(--text-light);
                cursor: pointer;
                width: 100%;
                padding: 0.5rem;
                border-radius: 0.25rem;
                display: flex;
                align-items: center;
                justify-content: flex-start;
                transition: all var(--transition-speed) ease;
            }
            
            .toggle-btn-bottom:hover {
                background-color: rgba(255, 255, 255, 0.1);
                color: var(--white);
            }
            
            .toggle-btn-bottom i {
                margin-right: 0.75rem;
                min-width: 16px;
                text-align: center;
            }
            
            .toggle-text {
                font-size: 0.875rem;
                white-space: nowrap;
                opacity: 0;
                transition: opacity var(--transition-speed) ease;
            }
            
            .sidebar-expanded .toggle-text {
                opacity: 1;
            }
            
            /* Panel de usuario */
            .user-panel {
                padding: 0.75rem 1rem; /* Reducido de 1rem */
                border-top: 1px solid rgba(255, 255, 255, 0.1);
                background-color: var(--darker);
                display: flex;
                align-items: center;
                flex-shrink: 0; /* No permitir que se encoja */
            }

            .user-avatar {
                width: 36px;
                height: 36px;
                border-radius: 50%;
                background-color: var(--primary);
                display: flex;
                align-items: center;
                justify-content: center;
                color: white;
                font-weight: 600;
                flex-shrink: 0;
            }

            .user-info {
                margin-left: 0.75rem;
                opacity: 0;
                transition: opacity var(--transition-speed) ease;
                overflow: hidden;
            }

            .sidebar-expanded .user-info {
                opacity: 1;
            }

            .user-name {
                font-weight: 500;
                font-size: 0.875rem;
                white-space: nowrap;
                color: white;
            }

            .user-role {
                font-size: 0.75rem;
                color: var(--text-light);
                white-space: nowrap;
            }

            /* Contenido principal - CORREGIDO para mejorar transición */
            .main-content {
                flex: 1;
                margin-left: 70px;
                padding: 1.5rem;
                transition: margin-left var(--transition-speed) ease;
                overflow-x: hidden; /* Evitar desplazamiento horizontal */
                max-width: calc(100% - 70px); /* Asegurar que no exceda el espacio disponible */
                box-sizing: border-box; /* Incluir padding en el cálculo del ancho */
            }

            .sidebar-expanded ~ .main-content {
                margin-left: 240px;
                max-width: calc(100% - 240px); /* Ajustar cuando el sidebar está expandido */
            }

            /* Navbar superior - CORREGIDO */
            .top-navbar {
                background-color: var(--white);
                padding: 0.75rem 1.5rem;
                box-shadow: var(--shadow);
                margin-bottom: 1.5rem;
                border-radius: 0.5rem;
                display: flex;
                align-items: center;
                justify-content: space-between;
                width: 100%; /* Asegurar que ocupe el ancho completo */
                box-sizing: border-box; /* Incluir padding en el cálculo del ancho */
                overflow: visible; /* Evitar que los elementos se recorten */
            }

            .page-title {
                font-size: 1.25rem;
                font-weight: 600;
                color: var(--text-main);
                margin: 0;
            }

            .navbar-actions {
                display: flex;
                align-items: center;
                gap: 1rem;
                flex-wrap: nowrap; /* Evitar que los elementos se envuelvan */
                min-width: fit-content; /* Garantizar espacio mínimo necesario */
            }

            .navbar-icon {
                width: 36px;
                height: 36px;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                cursor: pointer;
                position: relative;
                color: var(--text-muted);
                transition: all var(--transition-speed) ease;
                flex-shrink: 0; /* Evitar que estos íconos se encojan */
            }

            .navbar-icon:hover {
                background-color: rgba(67, 97, 238, 0.1);
                color: var(--primary);
            }

            /* Responsividad - CORREGIDA */
            @media (max-width: 992px) {
                #sidebar {
                    position: fixed;
                    top: 0;
                    left: 0;
                    height: 100%;
                    z-index: 1000;
                    transform: translateX(-100%);
                    transition: transform var(--transition-speed) ease, width var(--transition-speed) ease;
                    width: 240px !important; /* Ancho constante en móvil para evitar problemas */
                }
                
                #sidebar.sidebar-collapsed,
                #sidebar.sidebar-expanded {
                    transform: translateX(0);
                }
                
                .main-content {
                    margin-left: 0 !important;
                    max-width: 100%;
                }
                
                .overlay {
                    position: fixed;
                    top: 0;
                    left: 0;
                    width: 100%;
                    height: 100%;
                    background-color: rgba(0, 0, 0, 0.5);
                    z-index: 999;
                    display: none;
                }
                
                .sidebar-expanded ~ .overlay {
                    display: block;
                }

                .notifications-panel {
                    width: 100%;
                    max-width: 320px;
                }
                
                .top-navbar {
                    padding: 0.75rem 1rem; /* Reducir el padding en pantallas más pequeñas */
                }
                
                .page-title {
                    font-size: 1.1rem; /* Reducir tamaño del título en pantallas pequeñas */
                }
            }
        </style>

        <?= $this->headScript() ?>
    </head>
    <body>
        <div class="app-container">
            <!-- Sidebar / Navbar lateral - CORREGIDO -->
            <div id="sidebar" class="sidebar-collapsed">
                <div class="sidebar-header">
                    <div class="logo-icon">
                        <i class="fas fa-chart-pie"></i>
                    </div>
                    <div class="logo-text">Lodoro Analytics</div>
                    <!-- Se ha quitado el botón de toggle superior -->
                </div>
                
                <ul class="nav-menu">
                    <li class="menu-header">Principal</li>
                    
                    <li class="nav-item">
                        <a href="<?= $this->url('home') ?>" class="nav-link active">
                            <div class="nav-icon">
                                <i class="fas fa-tachometer-alt"></i>
                            </div>
                            <span class="nav-text">Resumen de ventas</span>
                        </a>
                        <div class="tooltip-nav">Resumen de ventas</div>
                    </li>
                    
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <div class="nav-icon">
                                <i class="fas fa-table"></i>
                            </div>
                            <span class="nav-text">Tablas</span>
                        </a>
                        <div class="tooltip-nav">Tablas</div>
                    </li>
                    
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <div class="nav-icon">
                                <i class="fas fa-chart-bar"></i>
                            </div>
                            <span class="nav-text">Estadísticas</span>
                            <span class="badge-new">Nuevo</span>
                        </a>
                        <div class="tooltip-nav">Estadísticas</div>
                    </li>
                    
                    <div class="menu-divider"></div>
                    <li class="menu-header">Reportes</li>
                    
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <div class="nav-icon">
                                <i class="fas fa-file-alt"></i>
                            </div>
                            <span class="nav-text">Reportería</span>
                            <span class="notification-badge">3</span>
                        </a>
                        <div class="tooltip-nav">Reportería</div>
                    </li>
                    
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <div class="nav-icon">
                                <i class="fas fa-download"></i>
                            </div>
                            <span class="nav-text">Exportar</span>
                        </a>
                        <div class="tooltip-nav">Exportar</div>
                    </li>
                    
                    <div class="menu-divider"></div>
                    <li class="menu-header">Sistema</li>
                    
                    <li class="nav-item">
                        <a href="<?= $this->url('application', ['action' => 'marketplace-config']) ?>" class="nav-link">
                            <div class="nav-icon">
                                <i class="fas fa-plug"></i>
                            </div>
                            <span class="nav-text">Integraciones</span>
                        </a>
                        <div class="tooltip-nav">Integraciones</div>
                    </li>
                                        
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <div class="nav-icon">
                                <i class="fas fa-question-circle"></i>
                            </div>
                            <span class="nav-text">Ayuda</span>
                        </a>
                        <div class="tooltip-nav">Ayuda</div>
                    </li>
                    
                    <li class="nav-item">
                        <a href="#" class="nav-link" id="notificationsBtn">
                            <div class="nav-icon">
                                <i class="fas fa-bell"></i>
                            </div>
                            <span class="nav-text">Notificaciones</span>
                            <span class="notification-badge">5</span>
                        </a>
                        <div class="tooltip-nav">Notificaciones</div>
                    </li>
                </ul>
                
                <!-- Botón de expansión inferior -->
                <div class="menu-divider"></div>
                <div class="sidebar-bottom-toggle">
                    <button class="toggle-btn-bottom" id="toggleBtnBottom" title="Expandir/Colapsar">
                        <i class="fas fa-chevron-right"></i>
                        <span class="toggle-text">Expandir menú</span>
                    </button>
                </div>
                
                <!-- Panel de usuario -->
                <div class="user-panel">
                    <div class="user-avatar">
                        <i class="fas fa-user"></i>
                    </div>
                    <div class="user-info">
                        <div class="user-name">Admin</div>
                        <div class="user-role">Administrador</div>
                    </div>
                </div>
            </div>
            
            <!-- Overlay para dispositivos móviles -->
            <div class="overlay"></div>
            
            <!-- Panel de notificaciones -->
            <div class="notifications-panel" id="notificationsPanel">
                <div class="notifications-header">
                    <div class="notifications-title">Notificaciones</div>
                    <button class="close-notifications" id="closeNotifications">
                        <i class="fas fa-times"></i>
                    </button>
                </div>
                <div class="notifications-tabs">
                    <div class="notifications-tab active">Todas (5)</div>
                    <div class="notifications-tab">No leídas (3)</div>
                </div>
                <div class="notifications-content">
                    <div class="notification-item">
                        <div class="notification-icon notification-success">
                            <i class="fas fa-check"></i>
                        </div>
                        <div class="notification-content">
                            <div class="notification-title">Base de datos actualizada</div>
                            <div class="notification-desc">La sincronización se completó exitosamente</div>
                            <div class="notification-time">Hace 5 minutos</div>
                        </div>
                    </div>
                    <div class="notification-item">
                        <div class="notification-icon notification-info">
                            <i class="fas fa-info"></i>
                        </div>
                        <div class="notification-content">
                            <div class="notification-title">Nuevo reporte disponible</div>
                            <div class="notification-desc">El reporte de ventas de marzo está listo</div>
                            <div class="notification-time">Hace 2 horas</div>
                            <div class="notification-actions">
                                <button class="notification-btn">Ver reporte</button>
                            </div>
                        </div>
                    </div>
                    <div class="notification-item">
                        <div class="notification-icon notification-warning">
                            <i class="fas fa-exclamation-triangle"></i>
                        </div>
                        <div class="notification-content">
                            <div class="notification-title">Advertencia de sistema</div>
                            <div class="notification-desc">El espacio de almacenamiento está al 80%</div>
                            <div class="notification-time">Hace 5 horas</div>
                        </div>
                    </div>
                    <div class="notification-item">
                        <div class="notification-icon notification-danger">
                            <i class="fas fa-exclamation-circle"></i>
                        </div>
                        <div class="notification-content">
                            <div class="notification-title">Error de sincronización</div>
                            <div class="notification-desc">Fallo en la conexión con API externa</div>
                            <div class="notification-time">Ayer</div>
                            <div class="notification-actions">
                                <button class="notification-btn">Reintentar</button>
                            </div>
                        </div>
                    </div>
                    <div class="notification-item">
                        <div class="notification-icon notification-info">
                            <i class="fas fa-user"></i>
                        </div>
                        <div class="notification-content">
                            <div class="notification-title">Nuevo usuario registrado</div>
                            <div class="notification-desc">Maria López se unió al sistema</div>
                            <div class="notification-time">Hace 2 días</div>
                        </div>
                    </div>
                    <a href="#" class="notification-all">Ver todas las notificaciones</a>
                </div>
            </div>
            
            <!-- Overlay para notificaciones en móvil -->
            <div class="overlay-notifications" id="overlayNotifications"></div>
            
            <!-- Contenido principal -->
            <div class="main-content">
                <!-- Navbar superior -->
                <div class="top-navbar">
                    <h1 class="page-title"></h1>
                    <div class="navbar-actions">
                        <div class="navbar-icon" id="navNotificationsBtn" title="Notificaciones">
                            <i class="fas fa-bell"></i>
                            <span class="notification-badge">5</span>
                        </div>
                        <div class="navbar-icon" title="Perfil">
                            <i class="fas fa-user"></i>
                        </div>
                    </div>
                </div>
                
                <!-- Contenido de la página -->
                <?= $this->content ?>
            </div>
        </div>

        <?= $this->inlineScript()->prependFile($this->basePath('js/bootstrap.min.js')) ?>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>

        <!-- Script corregido para toggle y tooltips -->
        <script>
            document.addEventListener('DOMContentLoaded', function() {
                console.log("DOM cargado, inicializando sidebar...");
                
                // Referencias a elementos del DOM
                const sidebar = document.getElementById('sidebar');
                const toggleBtn = document.getElementById('toggleBtn');
                const overlay = document.querySelector('.overlay');
                const notificationsBtn = document.getElementById('notificationsBtn');
                const navNotificationsBtn = document.getElementById('navNotificationsBtn');
                const notificationsPanel = document.getElementById('notificationsPanel');
                const closeNotifications = document.getElementById('closeNotifications');
                const overlayNotifications = document.getElementById('overlayNotifications');
                
                // Verificación de elementos
                if (!sidebar) console.error("Elemento sidebar no encontrado");
                
                // YA NO EXPANDIMOS AUTOMÁTICAMENTE - Eliminada la función expandSidebarOnLoad()
                
                // Función para alternar el estado del sidebar
                function toggleSidebar() {
                    console.log("Toggle sidebar clicked");
                    
                    if (sidebar.classList.contains('sidebar-collapsed')) {
                        console.log("Expandiendo sidebar");
                        sidebar.classList.remove('sidebar-collapsed');
                        sidebar.classList.add('sidebar-expanded');
                        
                        // Actualizar el ícono del botón inferior
                        const toggleBtnIcon = document.querySelector('#toggleBtnBottom i');
                        if (toggleBtnIcon) {
                            toggleBtnIcon.classList.remove('fa-chevron-right');
                            toggleBtnIcon.classList.add('fa-chevron-left');
                        }
                        
                        // Actualizar el texto del botón inferior
                        const toggleText = document.querySelector('.toggle-text');
                        if (toggleText) {
                            toggleText.textContent = 'Colapsar';
                        }
                        
                        // Registrar el ancho después de expandir para debugging
                        console.log("Ancho del sidebar expandido:", getComputedStyle(sidebar).width);
                    } else {
                        console.log("Colapsando sidebar");
                        sidebar.classList.remove('sidebar-expanded');
                        sidebar.classList.add('sidebar-collapsed');
                        
                        // Actualizar el ícono del botón inferior
                        const toggleBtnIcon = document.querySelector('#toggleBtnBottom i');
                        if (toggleBtnIcon) {
                            toggleBtnIcon.classList.remove('fa-chevron-left');
                            toggleBtnIcon.classList.add('fa-chevron-right');
                        }
                        
                        // Actualizar el texto del botón inferior
                        const toggleText = document.querySelector('.toggle-text');
                        if (toggleText) {
                            toggleText.textContent = 'Expandir';
                        }
                        
                        // Registrar el ancho después de colapsar para debugging
                        console.log("Ancho del sidebar colapsado:", getComputedStyle(sidebar).width);
                    }
                }
                
                // Asignar la función al botón inferior
                const toggleBtnBottom = document.getElementById('toggleBtnBottom');
                if (toggleBtnBottom) {
                    toggleBtnBottom.addEventListener('click', toggleSidebar);
                }
                
                // Cerrar sidebar con overlay en dispositivos móviles
                if (overlay) {
                    overlay.addEventListener('click', function() {
                        sidebar.classList.remove('sidebar-expanded');
                        sidebar.classList.add('sidebar-collapsed');
                        toggleBtn.querySelector('i').classList.remove('fa-chevron-left');
                        toggleBtn.querySelector('i').classList.add('fa-chevron-right');
                    });
                }
                
                // Marcar menú activo según la ruta actual
                const currentPath = window.location.pathname;
                const navLinks = document.querySelectorAll('.nav-link');
                
                navLinks.forEach(link => {
                    const href = link.getAttribute('href');
                    if (href && currentPath.includes(href) && href !== '#') {
                        link.classList.add('active');
                    }
                });
                
                // Toggle del panel de notificaciones
                function toggleNotifications() {
                    notificationsPanel.classList.toggle('open');
                    overlayNotifications.classList.toggle('open');
                }
                
                if (notificationsBtn) {
                    notificationsBtn.addEventListener('click', function(e) {
                        e.preventDefault();
                        toggleNotifications();
                    });
                }
                
                if (navNotificationsBtn) {
                    navNotificationsBtn.addEventListener('click', function() {
                        toggleNotifications();
                    });
                }
                
                if (closeNotifications) {
                    closeNotifications.addEventListener('click', function() {
                        notificationsPanel.classList.remove('open');
                        overlayNotifications.classList.remove('open');
                    });
                }
                
                if (overlayNotifications) {
                    overlayNotifications.addEventListener('click', function() {
                        notificationsPanel.classList.remove('open');
                        overlayNotifications.classList.remove('open');
                    });
                }
                
                // Tabs de notificaciones
                const tabs = document.querySelectorAll('.notifications-tab');
                tabs.forEach(tab => {
                    tab.addEventListener('click', function() {
                        tabs.forEach(t => t.classList.remove('active'));
                        this.classList.add('active');
                    });
                });
                
                // Iniciar debug para confirmar ancho correcto
                console.log("Ancho inicial del sidebar:", getComputedStyle(sidebar).width);
                console.log("Clases del sidebar:", sidebar.className);
                
                // Comprobar si estamos en móvil
                const isMobile = window.innerWidth <= 992;
                console.log("Vista móvil:", isMobile);
                
                console.log("Inicialización de sidebar completada");
            });
        </script>
    </body>
</html>